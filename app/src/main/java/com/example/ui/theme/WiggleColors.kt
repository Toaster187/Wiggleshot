package com.example.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * The app's colour palette. Previously every one of these values was repeated as a raw
 * `Color(0xFF...)` literal dozens of times across the screens.
 */
object WiggleColors {
    val Background = Color(0xFF0F1115)
    val Surface = Color(0xFF161920)
    val SurfaceElevated = Color(0xFF1E2430)
    val SurfaceMuted = Color(0xFF232A38)
    val Outline = Color(0xFF232A38)
    val OutlineStrong = Color(0xFF3B485E)

    val Accent = Color(0xFF00FFCC)
    val Warning = Color(0xFFFFCC00)
    val Danger = Color(0xFFFF4D4D)

    val TextPrimary = Color.White
    val TextSecondary = Color(0xFFC4CFDD)
    val TextTertiary = Color(0xFF8A94A6)
    val TextMuted = Color(0xFF4E586E)
    val IconMuted = Color(0xFF3B4861)

    val Scrim = Color(0xAA000000)
    val OverlayStrong = Color(0xD90D0F12)
    val Overlay = Color(0x990D0F12)
    val OverlaySoft = Color(0xCC0D0F12)
}
